# Comma-separated browser origins allowed by CORS.
# Must include the exact Vercel production URL (scheme + host, no trailing slash).
ALLOWED_ORIGINS=https://your-app.vercel.app,http://localhost:3000
